dist: bionic
language: python
sudo: false
cache:
  pip: true
  directories:
    - eggs
python:
  - "2.7"
before_install:
  - virtualenv -p `which python` .
  - bin/pip install -r requirements.txt
install:
  - bin/buildout -N -t 3
script: bin/test
